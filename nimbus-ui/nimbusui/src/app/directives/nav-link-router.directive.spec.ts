import { NavLinkRouter } from './nav-link-router.directive';

describe('NavLinkRouter', () => {
  it('should create an instance', () => {
    const directive = new NavLinkRouter();
    expect(directive).toBeTruthy();
  });
});
